<?xml version="1.0" encoding="UTF-8"?>
<!--
  Pre-launch lockdown: empty by design.
  A fetched empty sitemap is a stronger "drop these URLs" signal to Search
  Console than a 404. Restore the URL list when re-opening the site.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
</urlset>
